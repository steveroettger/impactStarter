ImpactStarter
=============

Development
-----------
    # setup
    git clone git@github.com:steveroettger/impactStarter.git
    cd impactStarter
    bundle install
    rake db:create:all
    rake db:schema:load db:seed
    rake populate

    # run test
    rake
